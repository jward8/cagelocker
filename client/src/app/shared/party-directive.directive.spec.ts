import { PartyDirectiveDirective } from './party-directive.directive';

describe('PartyDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PartyDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
